# Secrets (keep .env.example tracked)
.env
.env.*
!.env.example

# Virtualenv and caches
.venv/
__pycache__/
*.pyc
.pytest_cache/
.mypy_cache/

# IDE / OS
.idea/
*.swp
.DS_Store

# Offline wheel cache and model weights / exports
wheelhouse/
*.pt
*.onnx
*.engine
*.weights
models/

# Training / run artifacts
runs/
cache/
tmp/

# Manual backups (superseded by git)
backups/

# Logs and runtime artifacts
logs/
.service.pid
data/runtime.json
# per-machine calibration; app recreates a default
data/calibration.json
